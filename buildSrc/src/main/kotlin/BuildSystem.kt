object BuildSystem {
    val buildConfig = BuildConfigField
    val build = Build
    val versions = Versions
    val libraries = Libraries
    val plugins = GradlePlugins
    val classPaths = ClassPaths
}